# Java-Math-Quiz
This is another quick project I made in Java to test myself, just like the other math quiz I made in the past. It uses two methods which iterate throughout lists to quiz the user and returns their values to be used as a final score / result.
